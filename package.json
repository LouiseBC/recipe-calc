{
  "name": "recipe_scraper",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "s": "node dist/index.js",
    "c": "rm -rf ./dist && tsc --diagnostics --pretty -p ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "recipe-ingredient-parser": "^1.5.3"
  },
  "devDependencies": {
    "@types/node": "^9.3.0"
  }
}
